from enum import Enum, auto

class Phase(Enum):
    MENU = auto()
    OPTIONS = auto()
    INTRO = auto()
    BUILD = auto()
    FLIGHT = auto()
    RESULTS = auto()
    CREDITS = auto()
    PILOTS = auto()
    STORY = auto()